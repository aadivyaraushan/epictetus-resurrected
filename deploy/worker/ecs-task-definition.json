{
  "_comment": [
    "ECS Fargate task definition for the agent worker. Register it with:",
    "  aws ecs register-task-definition --cli-input-json file://deploy/worker/ecs-task-definition.json",
    "then run it as a Service with desiredCount 1, so ECS restarts it if it dies.",
    "",
    "Fargate rather than Lambda because this is not a per-request workload: the",
    "worker registers with LiveKit and holds a WebRTC connection for the length of",
    "a call. It has to stay up. If it is off, the deployed link is dead.",
    "",
    "One task is enough for a demo. A LiveKit worker handles several concurrent",
    "rooms, and the ceiling here is the grader's patience, not throughput.",
    "",
    "Fill in ACCOUNT, REGION and the secret ARNs before registering.",
    "Keys are pulled from Secrets Manager rather than written into 'environment',",
    "because anything in 'environment' is visible to anyone who can describe the",
    "task -- including the LiveKit API secret, which can sign a token for any room.",
    "",
    "No inbound ports. The worker dials out to LiveKit; nothing dials in. That is",
    "why there is no load balancer here and no security group ingress rule."
  ],

  "family": "epictetus-worker",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],

  "cpu": "1024",
  "memory": "2048",
  "_sizing_note": "1 vCPU / 2 GB. The local voice-activity and turn-detection models are small onnx models but they do run on CPU in-process, and the 18 MB index is held in memory. Below 2 GB the container has been known to be killed while loading.",

  "executionRoleArn": "arn:aws:iam::ACCOUNT:role/ecsTaskExecutionRole",
  "taskRoleArn": "arn:aws:iam::ACCOUNT:role/epictetus-worker-task",

  "containerDefinitions": [
    {
      "name": "worker",
      "image": "ACCOUNT.dkr.ecr.REGION.amazonaws.com/epictetus-worker:latest",
      "essential": true,
      "command": ["python", "agent/main.py", "start"],

      "environment": [
        { "name": "LIVEKIT_URL", "value": "wss://YOUR-PROJECT.livekit.cloud" }
      ],

      "secrets": [
        { "name": "LIVEKIT_API_KEY", "valueFrom": "arn:aws:secretsmanager:REGION:ACCOUNT:secret:epictetus/livekit-api-key" },
        { "name": "LIVEKIT_API_SECRET", "valueFrom": "arn:aws:secretsmanager:REGION:ACCOUNT:secret:epictetus/livekit-api-secret" },
        { "name": "OPENAI_API_KEY", "valueFrom": "arn:aws:secretsmanager:REGION:ACCOUNT:secret:epictetus/openai-api-key" },
        { "name": "DEEPGRAM_API_KEY", "valueFrom": "arn:aws:secretsmanager:REGION:ACCOUNT:secret:epictetus/deepgram-api-key" },
        { "name": "ELEVENLABS_API_KEY", "valueFrom": "arn:aws:secretsmanager:REGION:ACCOUNT:secret:epictetus/elevenlabs-api-key" },
        { "name": "TAVILY_API_KEY", "valueFrom": "arn:aws:secretsmanager:REGION:ACCOUNT:secret:epictetus/tavily-api-key" }
      ],

      "_optional_secrets_note": "The live personal backend (Notion, Google Calendar) is optional. Leave its secrets out and every personal tool answers from the seeded demo week, which is what a visitor sees anyway.",

      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/epictetus-worker",
          "awslogs-region": "REGION",
          "awslogs-stream-prefix": "worker",
          "awslogs-create-group": "true"
        }
      },

      "healthCheck": {
        "_note": "Checks the process is alive and its imports still work. There is no HTTP port to probe -- a LiveKit worker exposes none.",
        "command": ["CMD-SHELL", "python -c \"import agent.main\" || exit 1"],
        "interval": 30,
        "timeout": 5,
        "retries": 3,
        "startPeriod": 90
      },

      "stopTimeout": 30,
      "_stop_note": "Long enough for an in-progress call to end rather than cut off mid-sentence on a deploy."
    }
  ]
}
