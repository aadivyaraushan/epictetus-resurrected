# Merged Design and Notion Production Release

**Date:** 2026-07-31

## Purpose

Record the production release that combines the approved GAN design with the
single-database Notion connection fix, so a later deployment does not replace
one behavior with the other again.

## Result

DEPLOYED AND VERIFIED

- Merge commit: `35ab68e00da82a55a90de6e3f5a56bdc8a707199`
- Design parent: `cee47e1480494cc7a93c5bc72c9bc5a263abb57e`
- Notion-fix parent: `9099eca3e0277d8c4883acd98c5d218c28473cce`
- Vercel project: `aadivyaraushans-projects/epictetus-resurrected`
- Production deployment: `dpl_9DnaBHfarxzsnuMUJkTuPiYVqnxF`
- Production URL: https://epictetus-resurrected.vercel.app
- Rollback deployment: `dpl_GWiuoc8HPWA5Hd7vEb5j9smyJdqL`

The repository has no Git remote, so the release used an isolated local merge
and the existing direct Vercel deployment path. No Git push or pull request was
possible or attempted.

## Local Verification

```text
focused tests: 23/23 passed across 4 files
full tests:    55/55 passed across 14 files
TypeScript:    npx tsc --noEmit passed
build:         npm run build passed; all 8 application/API routes emitted
judge:         PASS; no required fixes
```

The approved live-call, completed-review, and design-contract files have no diff
from GAN design commit `6e8f673`. The merge changed the start-screen Notion area
and Notion/review routes but did not alter those approved screens.

## Production Verification

- Vercel state: Ready; the stable alias resolves to the new deployment.
- Home: HTTP 200 in 0.847 seconds.
- Token route: HTTP 200 with a server URL, a token, and an HttpOnly
  `review_permit` cookie. Token and cookie values were suppressed.
- Desktop 1280 x 781: OG home present, zero `select` elements, no horizontal
  overflow, no console errors, and 1.264-second browser load.
- Mobile 375 x 826: OG home present, zero `select` elements, no horizontal
  overflow, no console errors, and both Start Call and Connect Notion visible.
- Notion: `/api/notion/connect` returns HTTP 307 to
  `api.notion.com/v1/oauth/authorize` with a client ID, CSRF state, and the exact
  production callback URL. The clean browser reached Notion login, then stopped;
  no workspace authorization was granted.
- No paid voice call, Notion page write, or paid model call was made.

Screenshots:

- `saved-results/merged-release/live-desktop.png`
- `saved-results/merged-release/live-mobile.png`
- `saved-results/merged-release/notion-redirect-mobile.png`

## Remaining Concern

`npm ci` reported three high-severity dependency findings. They were not changed
inside this merge because upgrading packages would add unrelated release risk.
Review them separately with `npm audit` and test any dependency updates before a
later deployment.

## Reuse

1. Start from local `main`; this repository currently has no remote.
2. Link `web/` to Vercel project `epictetus-resurrected`.
3. Run the focused tests, `npm test`, `npx tsc --noEmit`, and `npm run build`.
4. Run `vercel deploy --prod --yes --scope aadivyaraushans-projects`.
5. Inspect the stable alias, check `/api/token` without printing its response
   secrets, and repeat desktop/mobile/browser redirect checks.
6. If the canary fails, restore the recorded previous Ready deployment.
