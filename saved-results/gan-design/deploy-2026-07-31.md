# GAN design production release

**Date:** 2026-07-31

## Purpose

Record how the approved GAN design, including the restored original home, was
committed, merged, deployed, and checked on the live Vercel application.

## Result

The local `main` baseline was committed at `9af534b`. The verified design was
committed at `6e8f673` and merged into local `main` at `13bd0d6`. The repository
has no Git remote, so there was no external Git push. The merged `main` web tree
was deployed directly to the existing Vercel Hobby project.

- Live URL: https://epictetus-resurrected.vercel.app
- Deployment ID: `dpl_8FX18fiC83PmHsUoVo2NkK2yCo89`
- Vercel state: `READY`
- Production alias: assigned
- Rollback source: previous Ready deployment `dpl_GWiuoc8HPWA5Hd7vEb5j9smyJdqL`

## Verification

- `npm test`: 50/50 passed across 13 files.
- `npx tsc --noEmit`: passed.
- `npm run build`: passed locally and in Vercel.
- Production HTTP response: 200 in 1.09 seconds.
- Browser load: 1.274 seconds with no console errors.
- Desktop 1280 x 781: OG home present, no horizontal overflow.
- Mobile 375 x 826: OG home present, no horizontal overflow.
- The live canary did not start a paid voice call.

Live screenshots:

- `saved-results/gan-design/deploy/live-desktop.png`
- `saved-results/gan-design/deploy/live-mobile.png`

## Reuse

This Vercel project is linked locally at `web/.vercel/project.json` in the main
worktree. A future direct production release can run `vercel deploy --prod`
from `web/`, followed by browser and deployment-status checks. Configure a Git
remote first if future releases must also be pushed to GitHub.
